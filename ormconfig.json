{
  "type": "mariadb",
  "host": "localhost",
  "port": 3307,
  "username": "root",
  "password": "root",
  "database": "hairfashion",
  "synchronize": true,
  "logging": false,
  "entities": ["src/models/**/*.ts"],
  "migrations": ["src/migration/**/*.ts"],
  "cli": {
    "migrationsDir": "src/migration"
  }
}
